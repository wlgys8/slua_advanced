
LBehaviour = {

AddTo = function (gameobject,tb)
	local mono = gameobject:AddComponent(LuaMonoBehaviour)
	mono.receiver = {'+=',function ( methodName,params )
		if not tb[methodName] then
			return
		end
		tb[methodName](unpack(params))
	end}
	return mono
end

}